<?php
// include ("Config.php");

// $sql = "SELECT ID, username, Password FROM admin";
// $result = $conn->query($sql);

// if ($result->num_rows > 0) {
//     while ($row = $result->fetch_assoc()) {
//         $user_id = $row['ID'];
//       //   $username = $row['username'];
//         $plaintext_password = $row['Password'];
        
//         $hashed_password = password_hash($plaintext_password, PASSWORD_BCRYPT);
//         $updateSqlPass = "UPDATE admin SET Password = '$hashed_password' WHERE ID = $user_id";
//         $conn->query($updateSqlPass);
//     }

// } else {
//     echo "No records found in the database.";
// }

?>

